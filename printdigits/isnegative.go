package main

import "piscine"

func IsNegative(nb int) {
	if nb >= 0;
		return z01.PrintRune(rune(84))
	else;
		return z01.PrintRune(rune(70))
}

func main() {
	piscine.IsNegative(1)
	piscine.IsNegative(0)
	piscine.IsNegative(-1)
}